import { ConvertMilToKmPipe } from './convert-mil-to-km.pipe';

describe('ConvertMilToKmPipe', () => {
  it('create an instance', () => {
    const pipe = new ConvertMilToKmPipe();
    expect(pipe).toBeTruthy();
  });
});
